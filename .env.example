# Hybrid AI Service - переменные окружения
# Скопируйте этот файл в .env и заполните своими API ключами

# ==============================================
# === РОССИЙСКИЕ AI СЕРВИСЫ (SFW) ===
# ==============================================

# === YANDEX GPT (для чата и анализа) ===
# Получите на: https://console.cloud.yandex.ru/
# Используется для безопасного (SFW) контента
YANDEX_API_KEY=your_yandex_gpt_api_key_here
YANDEX_FOLDER_ID=your_yandex_folder_id_here

# === YANDEX SPEECH KIT (для синтеза речи) ===
# Получите на: https://console.cloud.yandex.ru/
# Используется для генерации речи во всех режимах
YANDEX_SPEECH_KEY=your_yandex_speech_kit_api_key_here

# ==============================================
# === GOOGLE GEMINI (NSFW) ===
# ==============================================

# === GEMINI API (для откровенного контента) ===
# Получите на: https://makersuite.google.com/app/apikey
# Используется для NSFW контента (18+)
# ВАЖНО: Может потребовать VPN для доступа из России
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-pro

# === НАСТРОЙКИ БЕЗОПАСНОСТИ GEMINI ===
# Уровни блокировки контента (BLOCK_NONE/BLOCK_LOW/BLOCK_MEDIUM/BLOCK_HIGH)
# Для NSFW режима рекомендуется BLOCK_NONE
GEMINI_SAFETY_LEVEL=BLOCK_NONE

# ==============================================
# === НАСТРОЙКИ ГИБРИДНОГО РЕЖИМА ===
# ==============================================

# === АВТОМАТИЧЕСКАЯ КЛАССИФИКАЦИЯ КОНТЕНТА ===
# Включить автоматическое определение SFW vs NSFW (true/false)
HYBRID_AUTO_CLASSIFY=true

# Чувствительность классификатора (high/normal/low)
# high - более строгий анализ, больше ложных срабатываний
# low - более мягкий анализ, может пропустить NSFW
HYBRID_CLASSIFIER_SENSITIVITY=normal

# === МАРШРУТИЗАЦИЯ ЗАПРОСОВ ===
# Приоритет сервисов при равных условиях
# "russian_first" - сначала российский AI, потом Gemini
# "gemini_first" - сначала Gemini, потом российский AI
HYBRID_ROUTING_PREFERENCE=russian_first

# Таймаут запросов в миллисекундах
HYBRID_TIMEOUT_MS=30000

# Количество попыток повтора при ошибке
HYBRID_RETRY_ATTEMPTS=2

# === FALLBACK НАСТРОЙКИ ===
# Включить автоматическое переключение между сервисами (true/false)
HYBRID_FALLBACK_ENABLED=true

# Что делать если российский AI недоступен (use_gemini/use_russian/none)
HYBRID_IF_RUSSIAN_FAILS=use_gemini

# Что делать если Gemini недоступен (use_russian/use_gemini/none)
HYBRID_IF_GEMINI_FAILS=use_russian

# Логировать неудачи для отладки (true/false)
HYBRID_LOG_FAILURES=true

# === ОТЛАДКА ===
# Подробные логи маршрутизации (true/false)
HYBRID_DEBUG_ROUTING=false

# Логи классификации контента (true/false)
HYBRID_DEBUG_CLASSIFICATION=false

# Логи fallback операций (true/false)
HYBRID_DEBUG_FALLBACKS=false

# === СТАРЫЕ КЛЮЧИ (для обратной совместимости) ===
# Оставьте пустыми или укажите для совместимости
API_KEY=

# === НАСТРОЙКИ ПРИЛОЖЕНИЯ ===
# Режим работы приложения (development/production)
NODE_ENV=development

# Порт для разработки (по умолчанию Vite использует 5173)
VITE_PORT=5173

# Базовый URL для API (если используется бэкенд)
# VITE_API_URL=http://localhost:3001

# === ОТЛАДКА ===
# Включить подробные логи (true/false)
VITE_DEBUG=false

# === ПРОДАКШЕН НАСТРОЙКИ ===
# Домен для продакшена (измените на ваш домен)
# VITE_APP_URL=https://your-domain.com

# Включить сжатие и оптимизацию
VITE_ENABLE_COMPRESSION=true